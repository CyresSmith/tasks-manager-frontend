import styled from '@emotion/styled';
import myTheme from 'myTheme';

export const TasksList = styled.ul`
  margin-top: ${myTheme.space[4]};
  padding-top: ${myTheme.space[4]};
  border-top: 1px solid ${myTheme.colors.white};
`;
